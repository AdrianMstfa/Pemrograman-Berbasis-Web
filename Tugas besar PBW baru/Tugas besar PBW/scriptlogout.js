function logout() {
    // Logika untuk logout pengguna
    // Misalnya, menghapus token autentikasi, mengarahkan ke halaman login, dll.
    alert('Anda telah logout.');
    // Contoh pengalihan ke halaman login
    window.location.href = 'login.html';
}
